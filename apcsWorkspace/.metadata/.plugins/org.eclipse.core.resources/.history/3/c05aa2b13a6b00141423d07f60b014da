/*
Cody Brooks
2014-11-12
*/

public class matrixmath {

  public static void main (String[] args){
	  int[][] A = {
			  		  {2,5,7},
					  {7,3,9},
					  {7,2,8}
	  			  };
	  int p = 2;
	  System.out.println("Scalar addition of A + p" + MatMath.add(A, p));
  }

}

class MatMath {

  /*Scalar Addition*/
  static int [][] add (int p, int[][] A){
    int[][] result = new int[A.length][A[0].length];

    for (int i = 0; i < A.length; i++) {
      for (int j = 0; j < A[i].length; j++) {
        result[i][j] = p + A[i][j];
      }
    }

    return result;
  }

  /*Scalar Addition*/
  static int [][] add (int[][] A, int p){
    int[][] result = new int[A.length][A[0].length];

    for (int i = 0; i < A.length; i++) {
      for (int j = 0; j < A[i].length; j++) {
        result[i][j] = p + A[i][j];
      }
    }

    return result;
  }

  /*Scalar Multiplicaion*/
  static int [][] mult (int p, int[][] A){
    int[][] result = new int[A.length][A[0].length];

    for (int i = 0; i < A.length; i++) {
      for (int j = 0; j < A[i].length; j++) {
        result[i][j] = p * A[i][j];
      }
    }

    return result;
  }

  /*Scalar Multiplicaiton*/
  static int [][] mult (int[][] A, int p){
    int[][] result = new int[A.length][A[0].length];

    for (int i = 0; i < A.length; i++) {
      for (int j = 0; j < A[i].length; j++) {
        result[i][j] = p * A[i][j];
      }
    }

    return result;
  }

  /*Matrix Addition*/
  static int [][] add (int[][] A, int[][] B){
	int[][] result = new int[A[0].length][B.length];
    for (int i = 0; i < A.length; i++) {
        for (int j = 0; j < A[i].length; j++) {
          /*Perform Matrix multiplication here*/
        }
      }
	  return result;
  }

  /*Matrix Multiplication*/
  static int [][] mult (int[][] A, int[][] B){
	int[][] result = new int[A[0].length][B.length];
    for (int i = 0; i < A.length; i++) {
        for (int j = 0; j < A[i].length; j++) {
          /*Perform Matrix addition here*/
        }
      }
	  return result;
  }

}
